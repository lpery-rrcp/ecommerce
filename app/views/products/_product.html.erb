<h3><%= link_to product.name, product_path(product) %></h3>
<p><%= number_to_currency(product.price) %></p>

<%= button_to "Add to Cart", add_to_cart_path(product), method: :post, class: "btn btn-sm btn-success" %>
